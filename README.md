# api-cookie-config

```shell
 git init
 git branch -m main

 git config user.name manhbv.giaminh
 git config user.email manhbv.giaminh@gmail.com
 git add .
 git commit -m 'api-cookie-config'

 git remote add origin git@github.com:ecsaas/api-cookie-config.git
 git push origin main
```
